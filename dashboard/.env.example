# ============================================================
# ESSENTIELLA MILJÖVARIABLER
# ============================================================

# ---- DuckDB-databas ----
# Gemensam databasfil som både Dagster och Streamlit använder.
DUCKDB_PATH=/data_warehouse/job_ads.duckdb
STREAMLIT_DUCKDB_PATH=/data_warehouse/job_ads.duckdb

# ---- Dagster (orkestrering) ----
DAGSTER_HOST=0.0.0.0
DAGSTER_PORT=3000

# ---- dbt (datatransformation) ----
# Ange sökvägen till din dbt-profil i containern.
DBT_PROFILES_DIR=/opt/pipeline/data_transformation

# ---- Streamlit (dashboard) ----
STREAMLIT_HOST=0.0.0.0
STREAMLIT_PORT=8501

# ---- Azure (lägg till dina egna uppgifter) ----
# Dessa används vid deployment till Azure.
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
AZURE_CONTAINER_NAME=
AZURE_KEY_VAULT_URI=
